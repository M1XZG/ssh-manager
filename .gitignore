# ---- Python build / distribution artifacts ----
build/
dist/
*.egg-info/
.eggs/

# ---- Python cache / bytecode ----
__pycache__/
*.py[cod]
*$py.class

# ---- Virtual environments ----
.venv/
venv/
.env/
.envrc

# ---- Test / coverage artifacts ----
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ---- Linters / type checkers caches ----
.mypy_cache/
.ruff_cache/
.pyre/
.pytype/

# ---- IDE / editor ----
.vscode/
.idea/
*.swp
*.swo

# ---- Logs / temp ----
*.log
*.tmp
*.bak
*.orig

# ---- OS / misc ----
.DS_Store
Thumbs.db

# ---- Distribution of compiled extensions (none yet, but future-proof) ----
*.so

# ---- Local SSH working artifacts (avoid accidental inclusion if tests create them) ----
# (Tests & dev may create a fake HOME under tmp; real ~/.ssh never ends up here, but just in case)
ssh_temp_test_*/

# ---- Textual / app run caches (if any appear) ----
*.sqlite3

# ---- Security: never commit private keys (pattern safeguard) ----
*.pem
*.key
id_rsa
id_rsa.pub
id_ecdsa
id_ecdsa.pub
id_ed25519
id_ed25519.pub

# Allow the package metadata we actually need (negate if overly broad rules added later)
!src/
!tests/
!pyproject.toml
!README.md
!TODO.md
